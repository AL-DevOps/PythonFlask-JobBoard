node('built-in') {
    stage('checkout code') {
       checkout([$class: 'GitSCM', branches: [[name: '*/master']], extensions: [], userRemoteConfigs: [[credentialsId: 'github-privat-key-161022', url: 'git@github.com:AL-DevOps/PythonFlask-JobBoard.git']]])
   }
   stage('build') {
       sh '''docker build -t flask:latest . '''
   }
}
